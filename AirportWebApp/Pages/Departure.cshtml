@page
@model AirportWebApp.Pages.DepartureModel
@{
    ViewData["Title"] = "Departure Flight Status";
}

<h2>@ViewData["Title"]</h2>

<table class="table table-striped table-bordered">
    <thead class="table-dark">
        <tr>
            <th>Flight</th>
            <th>Destination</th>
            <th>Departure Time</th>
            <th>Gate</th>
            <th>Status</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var flight in Model.Flights)
        {
            <tr>
                <td>@flight.FlightNumber</td>
                <td>@flight.Destination</td>
                <td>@flight.DepartureTime.ToString("HH:mm")</td>
                <td>@flight.Gate</td>
                <td>
                    @if (flight.Status == "On Time")
                    {
                        <span class="badge bg-success">@flight.Status</span>
                    }
                    else if (flight.Status == "Delayed")
                    {
                        <span class="badge bg-warning text-dark">@flight.Status</span>
                    }
                    else if (flight.Status == "Boarding")
                    {
                        <span class="badge bg-primary">@flight.Status</span>
                    }
                    else
                    {
                        <span class="badge bg-secondary">@flight.Status</span>
                    }
                </td>
            </tr>
        }
    </tbody>
</table>
    